services:
  server:
    image: textract-image
    container_name: textract-container
    build:
      context: .
    ports:
      - 8000:8000
    develop:
      watch:
        - action: rebuild
          path: ./src
        - action: rebuild
          path: ./requirements.txt
